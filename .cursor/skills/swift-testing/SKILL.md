---
name: swift-testing
description: >-
  Trigger when writing Swift Testing (@Test, #expect, #require), parameterized
  or async tests, confirmations, tags, or migrating off XCTest. No XCTest.
  Router — load the fundamentals reference before writing tests.
---

# Swift Testing

Glimpse uses Swift Testing only (no XCTest). Prefer Context7 for current Testing API.

## Load rule (required)

Load **[fundamentals](references/fundamentals.md)** once when applying this skill. For TCA `TestStore` patterns use `composable-architecture` testing references instead.
