import Foundation

/// Shared container for app, widget, and Share Extension.
/// Exact suite matches Architecture: `group.<bundle-id>`.
public enum GLIAppGroup {
    public static let identifier = "group.com.postoevie.Glimpse"
}
